import { TestingHomePageComponent } from "./testing-home-page.component"

describe(`${TestingHomePageComponent}`, () => {
  // given my service receives 4 appointements
  // i expect to see 4 appontments in the sidebar
  // and the ifrst one to be displayed by default

  test(``, (  ) => {
  	// given my service receives 4 appointements
  })

})
